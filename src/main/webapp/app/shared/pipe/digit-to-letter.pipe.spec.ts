import { DigitToLetterPipe } from './digit-to-letter.pipe';

describe('DigitToLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new DigitToLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
